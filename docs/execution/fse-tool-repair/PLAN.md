# FSE 最小工具修复计划（2026-09-24）

基线：98997bbb7fba3e9287d54654cd197fa644ec56be。目标不是完善报告，而是完成可验证的实现闭环。
官方全文截止：2026-10-02 AoE，核对来源 https://conf.researchr.org/track/fse-2027/fse-2027-papers 。截止不是工具已经可用的证据。

## 本轮执行顺序

1. P0：复现 Growth/Flow/Reachability 的证据传递、作用域或契约错误。先失败测试，再最小通用修复；不能把未建模语义强行判为已证明。不新增目标、规则扫掠或漏洞利用。
2. P0：建立自包含离线端到端回归：已建模有效输入、缺失证据、错绑定/冲突、安全边界都经过生产入口；只用受控合成事实，不借用真实项目标签。
3. P1：并行修复测试可运行性阻塞（已迁移skill、硬编码旧版本、可选大资产依赖）。区分普通单元测试和显式资产集成测试，不以skip掩盖实现失败。
4. P0：对冻结事实做有界兼容/下游回归。只有本轮实际重新执行的阶段计入结果；旧0/33不得充当新实验。若无可证通用上游bug，保留阻塞，不重写事实提高召回。
5. P0：冻结代码，真实回归，精简交接，普通commit/push并核验远端SHA。

## 工作分配与边界

- 协调者：生产集成、Reachability/流水线连接、端到端与验收。
- Agent A：测试路径与版本/资产依赖诊断修复；只拥有指定tests与测试辅助文件。
- Agent B：Growth/Flow的离线契约一致性与coverage归因；只拥有dosweb/growth与指定测试，production修改由协调者接手。
- 源资产frameworks/databases/poc/results只读。不得运行目标服务、PoC、压力请求、动态复现、remote模型或178-target。不读取或提交密钥/私有模型审计。
- 不改旧工作树或外层用户dirty改动；不reset/clean/force push/合并默认分支。

## 完成定义

工具修复：复现证据 + 通用补丁 + 正反例 + 生产边界集成 + 回归真实结果。
科研可用：固定分母评价、真实适用性质消费、配对消融、独立样本泛化均有证据；这不是靠单元测试数量替代的门槛。
本轮的合成通过数、冻结回放兼容数、真实检测指标分别报告。没有真实增益不称工具完成或论文实验完成。

## 后续工作队列（非后台承诺）

实现冻结后才进入固定范围完整实验和配对消融；优先已有能力可覆盖的声明范围，不增开框架。论文只保留已验证方法与结果；未完成实验仍明确标记，不承诺具体完成时间或虚构结果。

## 本轮执行状态

Reachability和Growth记录正确性补丁、生产受控闭环、测试可移植性均已交付并冻结验收。最终1980 passed/66 skipped，原13失败分别为7通过与6明确缺失资产skip；下游冻结回放21/21且132unknown保持不变。47项新增回归不能当作真实召回增益。

A/B agent中断后不重启大任务；A由协调者接管，窄Growth记录修复由独立QA完成、协调者重验。新增一个独立静态源码验收，仅固定自有toy fixture原字节编译/既有查询，数据库与manifest源根必须一致；不执行Java程序、不接触canonical资产。结果逐次保留在reports/fse-tool-repair。

提交与推送状态以MCP最终交接中本地/远端SHA一致性记录为准。下一轮P0以summary.md的源码级闭环、维度/声明匹配和独立配对评价为限，不再横向扩展。
